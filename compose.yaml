services:
  db:
    image: postgres:18-alpine
    container_name: easySplitDb
    restart: unless-stopped
    environment:
      - POSTGRES_DB=${DB_NAME}
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    ports:
      - "5422:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data:


